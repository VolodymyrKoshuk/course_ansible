---
plugin: aws_ec2

regions:
  - eu-north-1

keyed_groups:
  - key: tags.Name
    prefix: tag_

  - key: tags.Role
    prefix: role_

  - key: instance_type
    prefix: instance_type
strict: False

hostnames:
  - private-ip-address
