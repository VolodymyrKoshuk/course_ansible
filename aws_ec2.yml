---
plugin: aws_ec2

aws_profile: default

regions:
  - eu-north-1

keyed_groups:
  - key: tags.Name
    prefix: tag_

  - key: instance_type
    prefix: instance_type
strict: False

hostnames:
  - ip-address
